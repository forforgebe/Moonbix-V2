export const app = {
    turboMode: false, // if true play fast mode (high risk ban) (experimental feature you might expect face a bug)
    maxScore: false, // if true always get maximum score
    antiDetect: true, // if true it always prevent all accounts will get ban while script is running
    maxRetry: 15, // retry amount for request fail
    key: "", // join t.me/erdropkosong for get access key
};
